 <div class="shadow-lg bg-white p-6 rounded-lg">
  <h1 class="text-2xl text-center py-5 font-bold text-gray-900 sm:text-3xl">Memberships</h2>
  <p class="mt-4 text-gray-500 sm:text-xl text-center">
       Memberships can now be purchased at reception in days, months, and years. Membership days will automatically be added to your user account.
      </p>
    </div>
<%#
<div class="mx-auto max-w-3xl px-4 py-8 sm:px-6 sm:py-12 lg:px-8">
  <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 sm:items-center md:gap-8">
    <% @memberships.each do |membership| %>
      <%# <div class="rounded-2xl border border-indigo-600 p-6 shadow-sm sm:px-8 lg:p-12">
        <div class="text-center">
          <h2 class="text-lg font-medium text-gray-900">
            <%= membership.name %>
            <%# <span class="sr-only">Plan</span>
          </h2>
          <p class="mt-2 sm:mt-4">
            <strong class="text-3xl font-bold text-gray-900 sm:text-4xl"><%= humanized_money_with_symbol(membership.price) %></strong>
            <%# <span class="text-sm font-medium text-gray-700">/month</span>
          </p>
        </div>
        <ul class="mt-6 space-y-2">
          <li class="flex items-center gap-1">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
              class="h-5 w-5 text-indigo-700"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
            <span class="text-gray-700"></span>
          </li>
          <!-- Add more list items based on your needs -->
        </ul>
        <a
          href="<%= membership_path(membership) %>
          <%# class="mt-8 block rounded-full border border-indigo-600 bg-indigo-600 px-12 py-3 text-center text-sm font-medium text-white hover:bg-indigo-700 hover:ring-1 hover:ring-indigo-700 focus:outline-none focus:ring active:text-indigo-500"
        >
          Get Started
        </a>
      </div>
    <% end %>
  <%# </div>
</div>  %>
