<!-- app/views/top_ups/index.html.erb -->
<div class="container">
  <h1>Top-Up Options</h1>
  <div class="row">
    <% @plans.each do |plan| %>
      <div class="col-6 col-md-4">
        <div class="card1">
          <%= cl_image_tag("https://res.cloudinary.com/dh8uxggfc/image/upload/v1695016725/Mejiro/Logo_4_edn8nr.png", width: 50, height: 50) %>
          <!-- Display top-up details, e.g., photo, name, etc. -->
          <div class="card-body">
            <h5 class="card-title"><%= humanized_money_with_symbol(plan) %></h5>
            <!-- You may add more details here -->
            <%= link_to "Top-Up", top_up_checkout_path(plan), class: "btn btn-secondary btn-secondary-font", data:{:turbo=>false}%>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
